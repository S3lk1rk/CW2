 - name: A playbook to deploy your image from dockerhub to kubernetes, create  a service for my application and scale your deployment appropriately to allow for rolling updates.
   hosts: localhost
   user: ubuntu
   become: yes
   
   tasks:
   - name: A. deploy the kubectl from my dockerhub image
     become: no
     shell:
        cmd: kubectl create deployment kubernetes-bootcamp --image=mselkirk/courseworkdevops    
   
   - name: B. *
